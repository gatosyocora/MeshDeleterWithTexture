#pragma kernel CSMain

int Width;

StructuredBuffer<int> Data;
AppendStructuredBuffer<float2> DeletePos;

[numthreads(32,32,1)]
void CSMain (uint2 id : SV_DispatchThreadID)
{
	if(Data[id.y * Width + id.x] == 1) 
	{
		DeletePos.Append(float2(id.x, id.y));
	}
}
